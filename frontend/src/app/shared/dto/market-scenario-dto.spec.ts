import { MarketScenarioDTO } from './market-scenario-dto';

describe('MarketScenarioDTO', () => {
  it('should create an instance', () => {
    expect(new MarketScenarioDTO()).toBeTruthy();
  });
});
